# Echo 项目交接文档

> 最后更新：2026-08-05
> 适用 APK：`D:\Agent\Agent  workplace\Claude Code工作区\Echo\.claude\worktrees\v3-task1-onboarding\app\build\outputs\apk\debug\app-debug.apk`
> 当前分支：`worktree-v3-task1-onboarding`（已推送到 GitHub `origin/worktree-v3-task1-onboarding`）

---

## 1. 项目目标

**Echo** 是一款**完全离线**的 Android 即时回放录音应用（类似 NVIDIA ShadowPlay），核心特性：

- **实时环形缓冲**：始终在后台保留最近 N 分钟音频，用户按下"暂停"即可保存"刚刚说过的话"，边界声音绝不丢失
- **零网络**：不声明 `INTERNET` 权限，所有数据永不上传，纯本地存储
- **公共目录备份**：长期录音可额外保存到 `Downloads/EchoBackup`，卸载不丢失，重装后可手动导入
- **密码保护**：支持 6 位数字 PIN / 图案密码，保护冷启动进入和删除操作
- **隐私优先**：开源可验签名，无账号、无追踪

---

## 2. 当前目录结构

项目在 GitHub 上的路径：`D:\Agent\Agent  workplace\Claude Code工作区\Echo\`，目前有 **3 个工作树**：

```
Echo/                                    ← 主目录，分支 main（稳定基线）
├── app/
│   ├── build.gradle.kts                 ← 构建配置（AGP 8.5.0, Kotlin 1.9.24）
│   └── src/main/
│       ├── AndroidManifest.xml          ← 权限：RECORD_AUDIO / FOREGROUND_SERVICE / POST_NOTIFICATIONS，无 INTERNET
│       ├── java/com/echo/recorder/
│       │   ├── EchoApplication.kt       ← Application，暴露全局 appContext()
│       │   ├── MainActivity.kt          ← 唯一 Activity，语言/主题入口
│       │   ├── EchoApp.kt              ← 根 Composable：引导流程 + 锁屏 + 服务绑定
│       │   ├── ServiceLocator.kt        ← 手动 DI 容器（Repository / VirtualRefStore 单例）
│       │   ├── service/
│       │   │   ├── RecordingService.kt  ← 前台录音服务（环形缓冲 FGS）
│       │   │   └── CircularBuffer.kt    ← N 分钟环形缓冲实现（segment 文件 + MediaExtractor→MediaMuxer 无损拼接）
│       │   ├── data/
│       │   │   ├── FilesystemRecordingDataSource.kt  ← 文件系统数据源（扫 pending/longterm/unprocessed 三目录）
│       │   │   ├── RecordingRepositoryImpl.kt        ← Repository 实现，合并虚引用
│       │   │   ├── VirtualRefStore.kt                ← 公共目录虚引用持久化（DataStore JSON）
│       │   │   └── RecordingDataSource.kt             ← 数据源接口
│       │   ├── domain/
│       │   │   ├── model/Recording.kt               ← Recording 数据类 + RecordingCategory 枚举
│       │   │   └── recording/RecordingRepository.kt  ← Repository 接口
│       │   ├── settings/
│       │   │   ├── SettingsRepository.kt  ← DataStore 设置（缓冲/密码/主题/语言/引导）
│       │   │   ├── PasswordCrypto.kt      ← 加盐 SHA-256（saltHex:hashHex）+ 6 位恢复密钥生成
│       │   │   └── ThemeMode.kt (在 SettingsRepository)  ← LIGHT/DARK/SYSTEM 枚举
│       │   ├── auth/
│       │   │   ├── SessionAuth.kt   ← 会话级门禁（isUnlocked/deleteUnlocked/savePublicUnlocked，onStop 重置）
│       │   │   └── LockoutManager.kt ← 错误次数锁定（3→30s / 5→1min / 7→3min）
│       │   ├── i18n/LocaleManager.kt  ← 语言切换（attachBaseContext + recreate）
│       │   ├── common/
│       │   │   ├── RecordingsDir.kt   ← 物理目录结构（pending/longterm/unprocessed/_buffer）
│       │   │   └── PublicDirManager.kt ← Downloads/EchoBackup 管理（29+ MediaStore / 26-28 直接写）
│       │   ├── playback/AudioPlayer.kt + MediaPlayerAdapter.kt + AudioPlayerFactory.kt ← 播放
│       │   ├── share/ShareHelper.kt   ← 文件分享（FileProvider）
│       │   ├── about/SignatureUtils.kt ← 读取自身签名 SHA-256
│       │   └── ui/
│       │       ├── navigation/EchoRoutes.kt + EchoNavHost.kt  ← 路由表
│       │       ├── theme/Theme.kt     ← Material 3 主题（primary 蓝/浅紫）
│       │       ├── record/RecordScreen.kt + RecordViewModel.kt  ← 录音主页
│       │       ├── list/ListScreen.kt + ListViewModel.kt        ← 录音列表（双 Tab + 多选）
│       │       ├── list/PublicDirManagementScreen.kt            ← 公共目录管理页
│       │       ├── settings/SettingsScreen.kt                  ← 设置页
│       │       ├── lock/ ← 密码体系 UI（LockScreen/PasswordSetupScreen/PasswordPromptDialog/PatternLockView/ChangePasswordDialog）
│       │       ├── onboarding/ ← 引导流程（LanguagePickerScreen/PrivacyAgreementScreen/OnboardingScreen）
│       │       ├── about/AboutScreen.kt
│       │       ├── player/PlayerViewModel.kt
│       │       └── Format.kt ← 时间/时长格式化
│       └── res/
│           ├── values/strings.xml     ← 中文
│           ├── values-en/strings.xml  ← 英文
│           ├── mipmap-*/ic_launcher.png + ic_launcher_round.png  ← 应用图标（5 密度）
│           └── xml/file_paths.xml     ← FileProvider 路径
└── .claude/worktrees/
    ├── v3/                            ← 分支 dev/sprint8，上一轮修正基线
    └── v3-task1-onboarding/           ← 分支 worktree-v3-task1-onboarding，**当前工作树**
```

---

## 3. 已完成的功能（已提交到 GitHub）

### 本轮提交（v3 发布前最终修正，7 个提交）

| 提交 | 内容 | 涉及文件 |
|---|---|---|
| `9873cd4` | **初始化流程重写**：step0 语言弹窗 → step1 隐私协议 → step2 6 张居中引导卡 → step3 主界面（才触发权限请求） | `EchoApp.kt`, `MainActivity.kt`, `onboarding/*` |
| `0d18807` | **通知栏修复**：回滚为标准 `NotificationCompat.Builder`，去掉 RemoteViews；BUFFERING 1 个暂停按钮，REVIEW 保存+删除，IDLE 无按钮；文案 4s 轮换无淡入 | `RecordingService.kt` |
| `26e3867` | **密码全流程**：图案/PIN 二选一、二次确认、6 位恢复密钥、`LockoutManager` 错误锁定、`ChangePasswordDialog`/`ResetRecoveryKeyDialog` | `auth/LockoutManager.kt`, `lock/*`, `SettingsScreen.kt` |
| `2fba4d1` | **公共目录手动导入**：保存到公共目录后从列表移除（不保留虚引用）；列表"导入"按钮扫描公共目录多选导入为虚引用；列表按钮按分类分离 | `ListScreen.kt`, `ListViewModel.kt`, `PublicDirManagementScreen.kt` |
| `e59e914` | **设置页完善**：隐私政策弹窗、"如何使用小E"复用 Onboarding 路由、主题/语言切换独立重建 Activity、录音列表按钮统一 | `SettingsScreen.kt`, `EchoNavHost.kt`, `EchoRoutes.kt` |
| `c4f5da2` | **缓冲重启修复**：改缓冲时长后保存同步等待完成再重启，移除 400ms 延迟，解决 0 秒空文件 | `RecordingService.kt` |
| `1749725` | **应用图标**：替换为 5 密度用户原图（48/72/96/144/192px） | `mipmap-*` |

### 上一轮基线（dev/sprint8，已合入 main）

完整功能：录音主页、环形缓冲、双 Tab 列表（临时/长期）、多选批量操作、设置页骨架、密码系统、公共目录虚引用、多语言（中/英）、主题切换、签名验证、分享、恢复弹窗、隐私协议、服务绑定。

---

## 4. 正在开发 / 未完成

**当前无进行中任务**。v3 修正已全部完成并推送到 GitHub。下一步候选见第 8 节。

---

## 5. 关键技术栈

| 层 | 技术 |
|---|---|
| 语言 | Kotlin 1.9.24 |
| UI | Jetpack Compose（BOM 2024.06.00）+ Material 3 |
| 构建 | AGP 8.5.0，Gradle Kotlin DSL，JVM 17 |
| 架构 | MVVM + StateFlow，`remember { ViewModel() }` 持有 |
| 存储 | DataStore Preferences（设置 + 虚引用 JSON） |
| 录音 | MediaRecorder 写段 + MediaExtractor→MediaMuxer 容器级拼接 |
| 服务 | ForegroundService（microphone），`START_STICKY` |
| 导航 | Navigation-Compose（单 Activity 多路由） |
| 语言 | 纯 ComponentActivity + `attachBaseContext` + `recreate()`（非 AppCompat） |
| 加密 | 加盐 SHA-256（`PasswordCrypto`），格式 `saltHex:hashHex` |
| 分享 | FileProvider（authority `${applicationId}.fileprovider`） |
| 测试 | JUnit4 + MockK + Turbine（协程流测试） |

**SDK 范围**：minSdk 26，targetSdk 34，compileSdk 34。

---

## 6. 重要文件说明（按职责）

### 入口与生命周期
- **`MainActivity.kt`**：唯一 Activity。`attachBaseContext` 用保存的语言包装 Context；`onCreate` 读主题应用；`onStop` 通过 `lifecycleObserver` 调 `SessionAuth.reset()`（会话边界）
- **`EchoApp.kt`**：根 Composable。控制引导流程（step 0-3）、冷启动锁屏、退出密码门控、服务绑定、恢复检查
- **`RecordingService.kt`**：前台录音 FGS。持有 `CircularBuffer`，暴露 `phase: StateFlow<Phase>`（IDLE/BUFFERING/REVIEW）和 `saving`。`onTaskRemoved`/`onDestroy` 做紧急保存（`cleanShutdown` 标记的彻底退出除外）

### 核心业务
- **`CircularBuffer.kt`**：环形缓冲实现。写 10s segment 到 `_buffer/`，维护滚动队列（默认 19 段 ≈ 3 分钟）。`save()` = 停录 → MediaExtractor 读各段 sample → MediaMuxer 写出一个完整 MPEG-4（**不重编码**，几乎瞬时）。返回拼接后毫秒时长
- **`RecordViewModel.kt`**：绑服务，暴露 `phase`/`saving`/`passwordEnabled`，`checkUnprocessed()` 做冷启动恢复
- **`ListViewModel.kt`**：双分类流、多选、`saveToPublic()`（复制到公共目录后删私有文件）、`scanImportable()`/`importFromPublicDir()`（虚引用导入）

### 数据层
- **`FilesystemRecordingDataSource.kt`**：物理目录即分类真相。`load()` 扫 `pending/`(临时)、`longterm/`(长期)、`unprocessed/`(未处理)，**不扫 `_buffer/`**。`setCategory()` 物理移动文件
- **`VirtualRefStore.kt`**：公共目录虚引用，DataStore JSON 持久化。文件在公共目录但要在列表显示时用
- **`SettingsRepository.kt`**：所有设置 Flow + setter。Keys 集中在内部 `Keys` 对象

### 密码体系
- **`PasswordCrypto.kt`**：加盐 SHA-256、`generateRecoveryKey()` 生成 6 位恢复密钥
- **`SessionAuth.kt`**：进程内会话门禁，`onStop` 重置
- **`LockoutManager.kt`**：错误计数锁定（3/5/7 次阶梯）
- **`PasswordSetupScreen.kt`**：类型选择 → 首次输入 → 二次确认 → 恢复密钥展示
- **`PasswordPromptDialog.kt`**：验证入口，集成锁定倒计时、忘记密码链接
- **`PatternLockView.kt`**：自定义图案锁，支持 `resetKey` 外部重置

### 公共目录
- **`PublicDirManager.kt`**：API 29+ 走 `MediaStore.Downloads`（分区存储），26-28 直接写 `Downloads/EchoBackup/`。`copyToPublic` / `scanPublic` / `deletePublic` / `findUri`

### 路由
- **`EchoRoutes.kt`**：`RECORD` / `LIST` / `SETTINGS` / `PASSWORD_SETUP` / `PUBLIC_DIR` / `ABOUT` / `ONBOARDING`
- **`EchoNavHost.kt`**：各路由 composable 装配，传 `viewModel` / 回调

---

## 7. 已知问题 / 待验证

| 问题 | 状态 | 备注 |
|---|---|---|
| 段切换有 <100ms 极小间隙 | 已知，可接受 | 语音场景不影响 |
| 恢复密钥仅展示一次，无二次确认 | 已知 | 用户需自行截图/备忘 |
| 公共目录导入无进度条 | 已知 | 文件少时感知不强 |
| 设置页"隐私模式"开关 disabled | 已知 | 占位，后续填充 |
| 应用未正式签名（debug 签名） | 已知 | 发布需填 `ECHO_*` 属性启用 release signing |
| 改缓冲时长后服务重启瞬间通知消失 | 已知 | 重启后立即重建 |
| 录音时长显示依赖 MediaMetadataRetriever，损坏文件回退 MediaExtractor | 已修复（2026-08-05） | 增加 (mtime→duration) 缓存，扫盘不重复创建 native 对象 |

---

## 7.5 2026-08-05 性能与稳定性修复（性能/bug 专线会话）

> 背景：UI 优化由独立会话负责（FluidWaveform/RecordScreen/SoftButtons 等视觉文件），本会话只动服务层/数据层/ViewModel 层。

| 修复 | 说明 |
|---|---|
| **CircularBuffer 并发竞态** | MediaRecorder 非线程安全：rollLoop(IO) 与 save/discard/release 可能在另一 IO 线程同时 stopRecorder/startSegment，并发调 rec.stop()/prepare() 会 IllegalStateException 或 native 崩溃。已用 `Mutex` 串行化所有对 recorder/segments 的操作；rollLoop 锁内二次确认仍在缓冲 |
| **startSegment 资源泄漏** | prepare() 失败时新建的 MediaRecorder 未 release；已补 release + 删除空文件 |
| **通知文案每 4s 阻塞读 DataStore** | `randomBufferingText()` 每次 `runBlocking` 新建 SettingsRepository 读语言，阻塞 IO 线程；改为语言变化时（observeLanguage）刷新缓存数组，轮换零阻塞 |
| **startBuffer 主线程 runBlocking + 连点竞态** | bufferSeconds 改为 onCreate 一次性读取并缓存；先置 phase=BUFFERING 再创建 CircularBuffer，防止连点重复创建 |
| **主线程长拼接** | handleRestart / saveUnprocessed / ACTION_KILL / deletePending 的 MediaMuxer 拼接 + repo 登记移到 `Dispatchers.IO`，主线程只同步等待（保证进程被杀前落盘） |
| **setCategory 数据丢失风险** | 原实现 copyTo 失败仍删原文件（磁盘满时丢数据）；改为 `renameTo` 优先（同分区原子移动），回退 copy 失败则不删原文件 |
| **load() NPE + 测试兼容** | 原实现 `context!!` 扫子目录，baseDir 模式下 NPE；改为基于 `dir` 扫根目录 + 分类子目录并跳过 `_buffer/`，同时兼容生产（context）与测试（baseDir）构造 |
| **时长读取缓存** | `readDurationMs` 每个文件建 MediaMetadataRetriever（native）；加 (mtime→duration) 缓存，文件未变不重读 |
| **PlayerViewModel 协程泄漏** | 默认 scope 无取消钩子，VM 清除后 collect 协程永不结束；onCleared 现在 cancel scope 再释放播放器 |
| **RecordViewModel.setRecorder 幂等** | 同一服务实例重复注入不再重复订阅 phase/saving |
| **FluidWaveform 编译错误** | 漏 import `luminance`（UI 会话遗留），导致整个项目无法编译；已补 import（未动视觉逻辑） |
| **过时测试修复** | 昨晚数据层重构后测试一直编译失败：FakeDataSource 补 setCategory、`RecordingRepositoryImpl.virtualRefs` 可空（默认 null）；删除测旧 API 的 RecordViewModelTest（Recorder/onRecordPressed 已不存在）；重写 EchoNavHostTest 匹配当前路由表 |

验证：**33 个单元测试全部通过（0 失败）**；`assembleDebug` 构建成功；桌面 `Echo-beta.apk` 已按部署习惯更新（2026-08-05 17:31）。

---

## 8. 下一步建议（按优先级）

1. **发布签名**：生成 release keystore，在 `gradle.properties` 填 `ECHO_STORE_FILE` / `ECHO_STORE_PASSWORD` / `ECHO_KEY_ALIAS` / `ECHO_KEY_PASSWORD`，取消 `app/build.gradle.kts` 中 `signingConfig` 注释
2. **合并到主分支**：当前改动在 `worktree-v3-task1-onboarding`，测试通过后合并回 `dev/sprint8` 或 `main`
3. **清理旧工作树**：`.claude/worktrees/v3/`（dev/sprint8）可删除
4. **隐私模式实现**：设置页占位开关，需定义具体行为（如隐藏通知内容）
5. **恢复密钥查看入口**：目前仅设置时展示一次，考虑在设置页加"查看恢复密钥"（需验证密码）
6. **录音时长显示**：如需显示准确时长，可引入 `MediaMetadataRetriever` 或从 `CircularBuffer` 计算
7. **单元测试覆盖**：`SettingsRepository` / `PasswordCrypto` / `LockoutManager` / `CircularBuffer` 可测

---

## 9. 不能改动 / 必须遵守的约束

> ⚠️ 这些是项目硬性约束，任何修改不得违反。

1. **绝不声明 `INTERNET` 权限**：`AndroidManifest.xml` 头部注释已写明，所有数据永不上传
2. **targetSdk=34 的 FGS 限制**：未授权时不能启动 microphone FGS，否则崩溃 → 权限请求必须在 step==3（主界面）才触发，不在 `MainActivity.onCreate`
3. **DataStore 单例**：`Context.settingsDataStore` 和 `Context.virtualRefStore` 通过 `preferencesDataStore` 委托创建，**不要**新建第二个实例
4. **物理目录即分类真相**：`pending/`、`longterm/`、`unprocessed/` 三个目录对应三个 `RecordingCategory`，不要引入额外分类维度
5. **会话边界是 `onStop`**：`SessionAuth.reset()` 在 `MainActivity.onStop` 调用，不要改到其他生命周期节点
6. **语言切换必须 `recreate()`**：纯 ComponentActivity 无法用 `AppCompatDelegate.setApplicationLocales`，必须走 `attachBaseContext` + `recreate`
7. **通知栏不要再用 RemoteViews**：标准 `NotificationCompat.Builder` + `addAction` 已稳定，自定义 View 在部分 OEM 会消失
8. **密码不可逆存储**：只存 `saltHex:hashHex`，不存明文；恢复密钥同样哈希存储
9. **公共目录 API 分级**：29+ 走 MediaStore，26-28 直接写，`PublicDirManager` 已封装，不要绕过
10. **Compose 编译器版本锁定**：Kotlin 1.9.24 必须用 `kotlinCompilerExtensionVersion = "1.5.14"`，不要升级

---

## 10. 常用命令

```bash
# 构建 debug APK
./gradlew.bat :app:assembleDebug

# 输出 APK 路径
ls app/build/outputs/apk/debug/app-debug.apk

# 运行单元测试
./gradlew.bat :app:testDebugUnitTest

# 查看当前分支
git branch --show-current

# 推送当前分支到 GitHub（需先开本地代理 127.0.0.1:10809）
git push -u origin worktree-v3-task1-onboarding
```

---

## 11. 关键路径速查

| 我要改… | 去哪个文件 |
|---|---|
| 录音主页 UI | `ui/record/RecordScreen.kt` |
| 录音逻辑 / 服务 | `service/RecordingService.kt` |
| 环形缓冲算法 | `service/CircularBuffer.kt` |
| 录音列表 | `ui/list/ListScreen.kt` + `ListViewModel.kt` |
| 设置页 | `ui/settings/SettingsScreen.kt` |
| 密码流程 | `ui/lock/PasswordSetupScreen.kt` + `PasswordPromptDialog.kt` |
| 图案锁 | `ui/lock/PatternLockView.kt` |
| 引导流程 | `ui/onboarding/*` + `EchoApp.kt`（step 状态机） |
| 语言切换 | `i18n/LocaleManager.kt` + `MainActivity.attachBaseContext` |
| 主题切换 | `ui/theme/Theme.kt` + `MainActivity.onCreate` |
| 公共目录 | `common/PublicDirManager.kt` |
| 设置存储 | `settings/SettingsRepository.kt` |
| 路由 | `ui/navigation/EchoRoutes.kt` + `EchoNavHost.kt` |
| 新增字符串 | `res/values/strings.xml` + `res/values-en/strings.xml`（**中英双写**） |

---

*文档结束。新对话接手时，请先读 `EchoApp.kt`（理解启动流程）和 `ServiceLocator.kt`（理解依赖关系），再按第 11 节路径定位修改点。*
